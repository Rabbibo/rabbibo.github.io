# Editable content source

These JSON files are the editable content source for the Rab's Labs site.

GitHub Pages serves the pre-rendered HTML files and does not run Flask or Jinja, so changing JSON here alone will not update the live pages. After editing content, regenerate the static export and upload the changed HTML along with this folder.
